{{!-- isolates one instance of volunteer experience when clicked on from the profile page(user handlebar)--}}



<section class="HoursContainer">
  {{!-- <img src="/images/{{painting.filename}}" alt="{{painting.description}}">
  <div class="painting-details"> --}}
    <h3> {{volunteerHours.name_organization}}Name of organization</h3>
    <p> Description of activity: {{volunteerHours.description}}</p>
    <p> Hours: {{volunteerHours.hours}}</p>
  </div>
</section>


{{!-- <section class="container">
  <h3 class="gallery-name">{{gallery.name}}</h3>
  <h4>Opening Hour: {{format_time gallery.starting_date}}</h4>
  <h4>Closing Hour: {{format_time gallery.ending_date}}</h4>
  <section class="flex-row justify-center">
    {{#each gallery.paintings as |painting|}}
    {{> painting-details}}
    {{/each}}
  </section>
</section> --}}